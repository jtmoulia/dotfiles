---
- name: install openssh
  become: yes
  package:
    name: "{{ ssh_package_name }}"
    state: "latest"

- name: create ~/.ssh/config.d directory
  file:
    path: "~/.ssh/config.d"
    state: "directory"

- name: copy in ssh config
  copy:
    src: "files/config"
    dest: "~/.ssh/config"
